func readString() ->
{
    __UPLBC("dwd tempString ")
    temp = readChar()

    while(temp > 10) ->
    {
        temp = itoa(temp)

        tempString = tempString + temp

        temp = readChar()
    }

    return tempString
}