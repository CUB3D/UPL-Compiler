//include "IO.call"
//include "Array.upl"
//include "File.upl"

//test = "B:\Git\UPL-Compiler\Array.upl"

//arrayReader = loadFileForReading(test)

//firstLine = readStringFromFile(arrayReader)

//println(firstLine)



    //for ( x = 0; x < 10; x++ )
       //{
       //    test--
       //
       //    print(x)
        //   print(test) // would not work if namespace is used
        //   //new syntax
        //   print(this::test)
        //   // this:: moves up the namespace stack by one
        //   this::this::test // recursive
       //

    //set x 0
    //whl x < 10
    //x++ // raw copy
    //psh x
    //jmp print
    //endwhl


x = 10
y = @TEMP1@
z = "Hello World"
abc = def[ghi]
__UPLBC("---")
test = testFunc(foo(bar(getTen())), "This is a test")
__UPLBC("---")
x = test(10)

//expected
//jmp getTen
//pop @TEMP@
//psh @TEMP@
//jmp bar
//pop @TEMP@
//psh @TEMP@
//jmp foo
//pop @TEMP@
